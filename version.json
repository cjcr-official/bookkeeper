{"version":115,"note":"Geocoding now proxies through your Bookkeeper Worker (server-side) — Google's Geocoding API rejects browser calls regardless of key restrictions because it sees the Referer header. The Worker uses your saved API key, so no extra setup."}
