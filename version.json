{"version":279,"note":"v279: the inline category manager now sits directly under the Category dropdown (moved inside the Category field) instead of spanning full-width below the row where it read as detached near the Account field."}
