{"version":124,"note":"Mileage cleanup: added a \"Maps\" button next to Calculate that opens the route in Google Maps (for cases where you need the exact Maps number — type it back in). Removed the Google API key from Settings entirely — Census + OSRM are free, no key, no APIs to enable. Dead OSM/ORS code in the client is gone."}
