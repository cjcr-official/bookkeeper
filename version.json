{"version":335,"note":"v335: Push reminders are now detailed — instead of a generic 'items due today', a job reminder names the job and time, and a recurring reminder names the item and amount. Payloads are encrypted (RFC 8291); if that ever fails it falls back to the old generic push so a reminder always lands."}
