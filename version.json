{"version":286,"note":"v286: App lock now auto-prompts Face ID the moment the lock screen appears (no tapping the button first). If iOS blocks the auto-attempt — e.g. a cold launch with no user gesture — it falls back silently to the passcode pad, with the 'Unlock with Face ID' button still there for a manual retry."}
