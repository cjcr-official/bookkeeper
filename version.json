{"version":439,"note":"v439: Fixed the loan Payment Calendar rendering with giant, stretched day cells (days scattered into alternating columns, the legend overlapping the grid). The square day cells sit in a grid whose 1fr columns kept an automatic minimum size that the cells' aspect-ratio inflated into huge squares; the columns now use minmax(0,1fr) with min-width:0 on the cells, and the grid is capped and centered so it stays a tidy phone-sized month grid on any screen."}
