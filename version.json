{"version":182,"note":"v182: reconcile — auto-matched records are now also excluded from cross-month search. Each render persists its auto-match fingerprints to the sidecar (only when changed), and other statements read them alongside manual matches."}
