{"version":94,"note":"Fixes: click-outside no longer leaves the scroll lock stuck; edit forms no longer show literal \"undefined\" when an optional field is empty; expense and account lists now escape names so a < in a vendor or account name doesn't break the row."}
