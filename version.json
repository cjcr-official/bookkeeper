{"version":278,"note":"v278: kill the one stacked modal — the expense-category 'Manage' link now toggles an inline panel inside the expense modal instead of opening a second overlay on top of it, so adding/removing a category no longer stacks dialogs or traps focus twice."}
