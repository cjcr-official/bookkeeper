{"version":409,"note":"v409: Fix Android/Samsung install — register the service worker on every launch and give it a fetch handler so 'Add to Home Screen' installs a real full-screen PWA (no address bar) with the app's logo instead of a generic bookmark. Added a static favicon link + a manifest id for a stable installed identity."}
