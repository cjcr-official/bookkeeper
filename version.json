{"version":476,"note":"v476: Bank reconciliation is simpler and steadier under the hood. The background 'learning' that occasionally made a month stop balancing is gone — which account a record clears through is now driven only by its 'Paid from' account, set automatically when you match it (or by hand). Your existing matches carry over. Multi-bank: if a record ever shows on the wrong account, matching it once fixes it for good."}
