{"version":138,"note":"Invoice editor: Total Miles now shows its decimal (e.g. 25.0). Tapping outside the invoice popup no longer closes it — only the X, Cancel, or Save do, so a stray tap can't discard an edit."}
