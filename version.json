{"version":472,"note":"v472: Removes the old 'Account' dropdown from the expense form (and from bulk edit), so the connected bank is the only account you pick. It was a leftover from the old chart-of-accounts page, which no longer exists — having two account fields side by side was just confusing. Nothing you'd already set is deleted."}
