{"version":167,"note":"v167: reconcile — fix splits/edits/matches not persisting. The sidecar JSON's upsert was silently failing when the storage policy only allows insert+delete (no update). Now falls back to delete-then-insert, and surfaces a real error toast instead of failing silently."}
