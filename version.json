{"version":116,"note":"Mileage calc now resolves addresses via Places API (New) searchText instead of the legacy Geocoding API — same dataset the Google Maps app uses, so distances match Maps. Requires enabling \"Places API (New)\" on your Google Cloud key (alongside Routes API)."}
