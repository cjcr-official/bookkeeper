{"version":317,"note":"v317: The Statements tab no longer \"pops\" when it opens. The connected-banks card takes a moment to load, and until now the content below it jumped down once it appeared. A correctly-sized placeholder holds its space while the bank card loads, and re-opening the tab keeps the card in place instead of flashing a loading state — so the page stays still."}
