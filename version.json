{"version":456,"note":"v456: Fixed the 'double ring' on focused loan-calculator fields — the bare input inside a .loan-field wrapper no longer paints its own glow ring inside the wrapper's focus ring. The wrapper now carries the single ring + glow (matching the paycheck inputs), so a focused Loan amount / Extra-per-month / rate field shows one clean outline instead of two concentric ones."}
