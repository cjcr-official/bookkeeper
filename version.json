{"version":366,"note":"v366: Expenses top section fixed — reuse the app's shared segmented .seg pill (full-width variant) so the This year/All time toggle matches the dashboard; dropped the poking red hairline on the summary card; grouped search + filters into one tidy panel."}
