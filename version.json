{"version":223,"note":"v223: manual matches on a Plaid-pulled month now save. Plaid statements have no PDF sidecar, so a month's matches/unmatches are stored per-month on the profile and re-applied on the next pull; transactions are ordered stably so match positions stay valid. Requires the new plaid_recon column."}
