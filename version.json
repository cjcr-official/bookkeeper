{"version":184,"note":"v184: reconcile fix — a record is no longer stolen by an adjacent month's loose auto-match. Auto-matches from other statements only suppress out-of-period records; the month that actually owns a record (e.g. a recurring $14.99 charge) keeps it. Manual matches still block everywhere."}
