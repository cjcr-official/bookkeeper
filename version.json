{"version":292,"note":"v292: fix the 2FA setup screen — the authenticator QR code now renders as a single clean image (was showing a broken-image box plus a stray duplicate QR because the SVG data-URI was concatenated into innerHTML)."}
