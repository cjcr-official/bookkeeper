{"version":449,"note":"v449: Sections switches are back on the card. Neither flex-shrink nor absolute positioning held on iOS Safari, so the section row is now a 2-column CSS grid — minmax(0,1fr) for the label, auto for the switch. The switch column is always sized to the switch and the label column absorbs what's left, so it can't be pushed off the card."}
